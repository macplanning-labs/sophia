# ============================================================
# .cargo/config.toml — クロスコンパイル設定（任意）
# ============================================================
# OSS 公開版は crates/ 配下の path 依存のみ。git 秘密リポジトリは不要。

[target.x86_64-unknown-linux-musl]
linker = "x86_64-linux-musl-gcc"
